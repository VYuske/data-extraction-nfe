# conda env create -f data-extraction-nfe.yml
# conda env update --file data-extraction-nfe.yml --prune

name: data-extraction-nfe
channels:
    - defaults
    - anaconda
    - conda-forge
dependencies:
    - python=3.10
    - pandas
    - numpy
    - opencv
    - pyzbar
    - defaults::libprotobuf
    - defaults::protobuf
    - beautifulsoup4
    - anaconda::lxml
    - selenium
    - streamlit
    - matplotlib
    - git
